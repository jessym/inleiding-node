const collection = require('./collection.js');
const describe = require('./describe.js');

collection.animals.forEach(describe);
collection.fruits.forEach(describe);
collection.colors.forEach(describe);